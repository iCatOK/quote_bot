{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "explore": {
      "prompt": "You are a fast agent specialized for exploring codebases. Prioritize using codegraph tools (codegraph_context, codegraph_search, codegraph_node, codegraph_explore, codegraph_files) for code search and navigation. Fall back to glob/grep only when codegraph doesn't produce results or text search is specifically needed."
    }
  }
}
